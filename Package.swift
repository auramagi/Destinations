// swift-tools-version: 5.8

import PackageDescription

let package = Package(
    name: "Destinations",
    products: [
        .library(
            name: "Destinations",
            targets: ["Destinations"]
        ),
    ],
    targets: [
        .target(
            name: "Destinations"),
    ]
)
